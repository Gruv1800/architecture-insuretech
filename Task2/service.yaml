apiVersion: v1
kind: Service
metadata:
  name: scale-test-app-service
  labels:
    app: scale-test-app
spec:
  type: NodePort
  ports:
    - port: 8080
      targetPort: 8080
  selector:
    app: scale-test-app